git clone --separate-git-dir=$HOME/.dotfiles https://github.com/ersatzryan/dotfiles.git $HOME/.dotfiles-tmp
rm -r ~/.dotfiles-tmp
alias dotfiles='/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'
dotfiles checkout .

sh ./.install.sh
